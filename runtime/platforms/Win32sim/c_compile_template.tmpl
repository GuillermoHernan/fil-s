c_compile.bat
cmd /C "${IntermediateDir}\c_compile.bat" >NUL

REM script starts here
call "H:\Program Files (x86)\Microsoft Visual Studio\2017\Community\VC\Auxiliary\Build\vcvars32.bat"
cd "${IntermediateDir}"

SET DRIVE=${IntermediateDir}
SET DRIVE=%DRIVE:~0,2%
%DRIVE%

cl "${CFilePath}" -nologo /FAs /Ox >"${IntermediateDir}\${ModuleName}.compiler.out"
