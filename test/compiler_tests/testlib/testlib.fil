/*
 * Library to support FIL-S compiler tests
 */

//Marks the test as successful and stops the program. 
function passed ()
{
	digitalOut (15,true);
	quit(0);
}

//MArks the test as failed and stops the program
function failed ()
{
	digitalOut (15,false);
	quit(-1);
}